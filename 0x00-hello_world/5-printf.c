#include <stdio.h>
/**
 * main - This is a program that can print a line using the print f function
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
