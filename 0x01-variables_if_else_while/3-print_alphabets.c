#include <std.h>
/**
 * main -print the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstun
		int i;

	for (i - 0; i < 26; i++)
	{
		putchar(alp[1])
	}
	putchar('\ n ');
	return (0);
